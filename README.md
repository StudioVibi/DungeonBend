# DungeonBend

Game source, assets, JSON data, and build pipeline for DungeonBend.

The local editor lives in the sibling repository:

```sh
cd ../DungeonBendEditor
bun run start
```

The editor targets this checkout by default. To point it somewhere else, run it with `GAME_ROOT=/path/to/DungeonBend`.
